import 'package:flutter/material.dart';

class ErrorRoutesWidget extends StatelessWidget {
  const ErrorRoutesWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Sai địa chỉ liên kết rồi")),
    );
  }
}
